<% if flash[:danger] %>
  <%= flash[:danger] %>
<% end %>

<%= form_for(:session, url: sessions_path) do |f| %>

  <%= f.label :EntityID %>
  <%= f.text_field :EntityID %>

  <%= f.submit "Log in", class: "btn btn-primary" %>
<% end %>
<%= debug(params) if Rails.env.development? %>